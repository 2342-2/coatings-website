# Only needed in production, or if your API runs on a different host/port.
# In local dev, Vite's proxy (see vite.config.js) handles this automatically.
# VITE_API_URL=https://maridadi-api.onrender.com
